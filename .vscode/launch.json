{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch app.py",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/app.py",  // Path til app.py
            "console": "integratedTerminal"
        }
    ]
}